const config = {
    app: {
        port    : 3000
    },
    db: {
        host    : '127.0.0.1',
        user    : 'username',
        password: 'password',
        port    : '27017',
        name    : 'database_name'// database name
    }
}

module.exports  = config;